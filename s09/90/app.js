function add(x, y) {
  return x + y;
}

const sum = function (x, y) {
  return x + y;
};

const product = function multiply(x, y) {
  return x * y;
};

console.log(add(4, 5));
console.log(sum(9, 3));
console.log(product(5, 3));
